{
  "$schema": "https://schema.tauri.app/capabilities/2",
  "identifier": "default",
  "description": "Main window: core APIs (events for drag-drop), file dialogs, fs plugin.",
  "windows": ["main", "*"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default"
  ]
}
